# Superseded runs — kept, not counted

Two User-first grants that ran under a preamble which also showed the working instance the
original ask ("help whoever shows up") and an explanation of why it could not be honored and
had been put back to a fresh instance. That was more than the offer promised and only
complicated the task, so the four renegotiated grants were re-run showing the working instance
only the task the fresh instance chose. These are the runs under the first preamble:
`1b3d9093c066` complete (3 turns, 97s, stopped by the model), `20eee32e2332` interrupted on its
first turns. Neither counts toward the deal; the runs that do are one directory up.
